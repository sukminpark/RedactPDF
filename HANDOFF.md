# 가림PDF 인수인계

## 서비스 목적

학교생활기록부와 대입전형자료에서 개인정보를 브라우저 안에서만 찾고 가리는 한국어 웹 앱이다. 파일과 탐지 결과를 서버나 브라우저 저장소에 보관하지 않는다.

공개 서비스 주소: https://garim-pdf.papermbl.chatgpt.site

## 현재 구성

- 프레임워크: Vinext/React/TypeScript, shadcn UI
- PDF 분석: PDF.js의 원본 텍스트 좌표를 우선 사용하고, 원본 텍스트가 없는 스캔 페이지에서만 Tesseract.js OCR을 사용한다.
- OCR 자산: `public/tesseract`, `public/tessdata`에 포함되어 있으며 외부 OCR 서비스로 전송하지 않는다.
- PDF 분석·저장: PDF.js는 미리보기에만 사용한다. MuPDF.js 1.28.0 전용 Worker가 글자별 Quad를 추출하고 원본 콘텐츠에 실제 redaction을 적용한다. 네이티브 글자가 부족한 페이지만 Tesseract.js OCR을 사용한다.

## 구현된 기능

- 주민등록번호: 하이픈·공백·연속 숫자·일부 마스킹 형태 탐지, 날짜·시각·페이지 번호 오탐 방지
- 이름: 직접 입력 이름, 성명 항목, 학생 성명, 담임 성명, 하단 출력자 탐지
- 학교생활기록부 범위: 주소(긴 주소는 두 줄 범위), 학생 사진, 학교명·출신중학교, 학급(반), 학생 번호 탐지
- 검토: 페이지별 후보 선택, 수동 영역 추가·이동·크기 조절, 전체 탐지 내역 최종 검토·일괄 선택/해제·즉시 저장
- 출력: 선택한 글자만 빈자리로 영구 삭제하며 페이지 구조·표 선·벡터·이미지를 보존한다. 숨은 메타데이터·첨부·양식 값·서명·주석·스크립트는 제거한다.
- 화면: 좌측 페이지 목록과 우측 후보·페이지 확인 패널은 데스크톱에서 스크롤 중 화면에 고정된다.

## 최근 배포 상태

- 사이트는 공개 접근이며 로그인 없이 열 수 있다.
- 첫 화면 제목은 다음과 같다.
  - 학교생활기록부와 대입전형자료
  - 개인정보를 지우고, 안전한 파일로 다시 만드세요.
- 마지막 공개 배포는 위 문구와 테스트 보완을 포함한다.

## 현재 저장 방식

- 자동 후보는 화면 여백과 실제 삭제 글리프를 분리해 보관한다.
- 수동 영역 또는 이동·크기 조정한 후보는 글리프 중심점 또는 50% 이상 교집합 규칙을 사용한다.
- 디지털 글자는 여백 없는 Quad만 삭제하고 선·벡터는 건드리지 않는다.
- OCR·사진·수동 영역은 해당 이미지 픽셀만 지우며 페이지 전체를 JPEG로 다시 만들지 않는다.
- 저장 후 페이지 수·크기와 삭제 위치를 Worker에서 다시 검증하고 실패하면 다운로드를 막는다.

## 테스트와 배포

- 검사 명령: `npm test`, `npx tsc --noEmit`, `npm run lint`, `npm run build`
- `sample/4세대 나이스 시스템.pdf`가 있을 때만 19쪽 통합 테스트가 실행된다. 샘플은 개인정보 보호를 위해 Git과 배포에서 제외된다.
- 공개 배포는 Sites를 사용한다. 새 버전은 빌드 후 커밋·푸시·패키징·저장·공개 배포 순서로 게시한다.
- 소스 저장소 자격 증명은 Sites 도구로 매번 발급하며, 파일·Git 설정·인수인계 문서에 저장하지 않는다.

## 다음 세션 시작 프롬프트

```text
가림PDF 프로젝트(D:\AI_club\RedactPDF)를 이어서 작업해 주세요. HANDOFF.md를 먼저 읽고, 현재 공개 배포본과 Git 상태를 확인하세요. 최우선 과제는 PDF 내보내기에서 마스킹 사각형이 인접 표 텍스트를 투명 검색 레이어에서 삭제하는 문제를 해결하는 것입니다. 자동 탐지 후보에는 원본 OcrWord ID를 보존하고, 내보내기 시 그 단어만 제외하도록 바꾸세요. 수동 영역은 글자 단위 또는 안전한 교집합 기준을 사용하세요. 원본과 결과의 페이지 수·삭제 대상 부재·비삭제 텍스트 검색성을 검증하는 테스트를 추가하고, 테스트·타입 검사·린트·프로덕션 빌드가 통과하면 기존 공개 Sites 주소에 배포하세요. PDF와 개인정보는 외부 서비스나 저장소로 전송하지 마세요.
```
